name: Deploy & Build Status

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Project
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Write Deployment Summary Details
        run: |
          echo "## 🚀 Deployment Status Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Resource | URL / Status |" >> $GITHUB_STEP_SUMMARY
          echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY
          echo "| 🌐 **Cloudflare Pages Production URL** | [https://dayarplus-website.pages.dev](https://dayarplus-website.pages.dev) |" >> $GITHUB_STEP_SUMMARY
          echo "| ⚡ **Live App System URL** | [https://dayarplus.knuriel.workers.dev/fees](https://dayarplus.knuriel.workers.dev/fees) |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 **GitHub Pages Mirror URL** | ${{ steps.deployment.outputs.page_url }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🏷️ **Commit SHA** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
